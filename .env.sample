# Copy to .env and fill in real values. .env is git-ignored; never commit it.
# Used by docker-compose.yml only.

POSTGRES_DB=acme_incidents
POSTGRES_USER=acme
POSTGRES_PASSWORD=<set-this-locally>

# Change if 5432 is already taken on your machine.
POSTGRES_PORT=5432
ADMINER_PORT=8080
